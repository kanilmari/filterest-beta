This folder contains reproduceable components, like special dropdowns and tree structures. Login, register, etc. aren't repeated on sites, so they don't belong here. 

Also e.g. navigation bar or filterbar options don't belong here, because site can just use one navigation bar and one filter bar at a time, no matter how many options.