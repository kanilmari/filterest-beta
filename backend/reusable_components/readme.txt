This folder contains reproduceable components, like special dropdowns and tree structures. 

While there can be multiple versions of Login, register, etc., there can be only one login/register on a site, so they don't belong here.